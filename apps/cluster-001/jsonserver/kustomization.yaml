apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: app-instance-01

resources:
  - namespace.yaml
  - ../../base/jsonserver

patches:
  - target:
      kind: JsonServer
      name: default-name
    patch: |
      - op: replace
        path: /spec/replicas
        value: 2
      - op: replace
        path: /metadata/name
        value: app-instance-01