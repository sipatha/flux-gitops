apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../base/jsonserver

patches:
  - target:
      version: server.vambita.com/v1alpha1
      kind: JsonServer
    patch: |
      - op: replace
        path: /spec/replicas
        value: 2